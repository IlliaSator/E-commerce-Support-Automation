[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["backend/tests", "bot/tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
    "B008",
    "E402",
    "E501",
    "UP017",
]
